# セキュリティ

## 脅威モデル

防御を作る前に、何から何を守るのかを先に書きます。ここに書いていないものは
**守る対象ではありません**。

| 項目 | 内容 |
|---|---|
| **誰から** | 席の AI。悪意を想定しているのではなく、指示の誤解 / prompt injection / 暴走を含む |
| **何を** | 議事録の内容と、「誰が何を言ったか」の帰属 |
| **どうなると困る** | 座長が偽の意見・偽の帰属に基づいて裁定する |
| **守らないもの** | OS レベルの侵害 / 座長自身の誤操作 / 席が自分の意見を盛ること（内容の真偽は evidence 型付けの担当で、検証ではない） |

### この設計で守れること

**議事録の改ざんは検知できます。** dispatcher は書き込み前に対象ファイルの working tree が
clean かを検査し、書き込み後に auto-commit します。dispatcher 以外の書き込みは
次の操作で dirty として現れ、diff を提示して停止します（fail-closed）。

**帰属は席ごとに分かれています。** 各意見は invocation ID と participant で議事録に
記録され、merge は ID で冪等です。

**最終的な証跡は origin に push した履歴**です。席に認証情報を渡さない限り、
席はリモート履歴に到達できません。

### この設計で守れないこと（明示）

**ローカル git 履歴の書き換え。** 席が任意コマンドを実行できる環境では、ローカルの
git 履歴そのものを書き換えられます。これは検知できません。押さえているのは
「push 済みの履歴と食い違えば分かる」という一点です。

**席の sandbox には依存していません。** 以前は「席は自分の作業ディレクトリの外に
書けない」という前提で、sha256 の控えを外側に置いていました。実測でこの前提は
破れており（Windows の Grok 席は PowerShell を任意実行できた）、控えごと書き換え
られるため検知が成立しませんでした。**現在の検知は sandbox の実効範囲に依存しません。**

**意見の内容の正しさ。** evidence 型付け（`observed` / `log` / `argument` など）は
席の自己申告です。検証済みという意味ではなく、そう表示もしません。

## 運用上の境界

- Tier2（UI 自動化）は席ごとの明示承認がない限り有効化されません。Tier1 が失敗しても
  **自動で Tier2 へ昇格することはなく**、Tier3（人間が貼付）へ落ちます
- dispatcher は `minutes/<議題>/` と一時領域の外に書きません
- 席に GitHub 等の認証情報を渡さないでください。渡すと最終証跡が成立しなくなります
- 議事録の root は git 管理下である必要があります。git の外に置くと検知が消えます
  （ツール側で検査しています）

## 未検証の前提

実測できていない前提を文章に書くときは、同じ commit で反証テストを `xfail` として置き、
`pytest -rx` で機械に列挙させる方針です（[CONTRIBUTING](CONTRIBUTING.md) の約束 2）。
xfail が 0 件でも「未検証の前提が無い」ことの証明にはなりません。既知の制限は
[README の制限節](README.md) に列挙しています。

## 脆弱性の報告

GitHub の Private Vulnerability Reporting（Security タブ → Report a vulnerability）で
非公開に報告してください（public 化時に有効化します）。使えない場合は Issue に
「脆弱性がある」という事実だけを書き、再現手順・認証情報・個人情報は貼らないでください。
