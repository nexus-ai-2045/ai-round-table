# ai-roundtable v0.1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 人間司会のマルチ AI 壁打ちを chat-first で回す dispatcher (packet 生成・watcher 回収・議事録 merge・journal) を Tier3 (人間 relay) で完成させ、spike 通過席のみ Tier1 自動 relay を足す。

**Architecture:** DESIGN v5 (docs/DESIGN.md) 準拠。dispatcher は AI を実行しない決定的 Python ツール。席 = アプリの実チャット。共有点は minutes/<topic>/ 配下のファイルのみ。

**Tech Stack:** Python 3.13 (стдlib のみ、外部依存なし) / pytest / Windows 優先 (パスは pathlib、クリップボードは clip.exe UTF-16LE)

## Global Constraints

- AI を subprocess で実行するコードを書かない (DESIGN v5 §0。relay は「届ける」だけ)
- 外部パッケージ依存なし (stdlib + pytest のみ)。schema 検証は手書き
- すべてのファイル書き込みは tmp → `os.replace` の atomic write
- `subprocess` 使用時は `encoding` 明示・`shell=False` (clip.exe は UTF-16LE bytes)
- コメント・docstring は日本語。識別子は英語。1 ファイル 400 行以内
- コミットは各 Task 末尾で行う (repo: `C:\Users\yas\Projects\Documents\.repos\nexus_ai\private\ai-roundtable`)
- python 実行は `C:\Users\yas\AppData\Local\Programs\Python\Python313\python.exe` を明示

## File Structure

```
roundtable/
  __init__.py
  paths.py        # topic ディレクトリ規約の一元管理
  schema.py       # 意見 JSON の検証 (手書き)
  minutes.py      # 議事録: 生成・snapshot・hash・merge (escape / 予約見出し防御)
  journal.py      # invocation 状態機械 + 冪等 merge 記録 + round 管理
  packet.py       # packet 文生成 + クリップボード搬出 (Tier3)
  watcher.py      # scratch 監視 → 検証 → merge 駆動
  cli.py          # new-topic / dispatch / status / close
  relay_ccd.py    # (Phase 2, spike 通過時のみ) CCD send_message アダプタ
  relay_codex.py  # (Phase 2, spike 通過時のみ) codex app-server JSON-RPC アダプタ
tests/
  test_schema.py test_minutes.py test_journal.py test_packet.py test_watcher.py test_e2e_mock.py
docs/PROTOCOL.md  # ホスト AI 向け手順書 (SSOT)
adapters/cc-skill/SKILL.md
```

---

## Phase 1 — Tier3 で完動する核

### Task 1: scaffolding + paths

**Files:**
- Create: `pyproject.toml`, `roundtable/__init__.py`, `roundtable/paths.py`, `tests/test_paths.py`

**Interfaces:**
- Produces: `paths.topic_dir(root: Path, slug: str) -> Path` / `paths.ensure_topic(root, slug) -> TopicPaths`
- `TopicPaths` dataclass: `.minutes` (minutes.md) `.journal` (journal.json) `.scratch` (dir) `.snapshot` (dir)

- [ ] **Step 1: pyproject.toml を書く**

```toml
[project]
name = "ai-roundtable"
version = "0.1.0"
requires-python = ">=3.13"

[tool.pytest.ini_options]
testpaths = ["tests"]
```

- [ ] **Step 2: failing test を書く** (`tests/test_paths.py`)

```python
from pathlib import Path
from roundtable.paths import ensure_topic

def test_ensure_topic_creates_layout(tmp_path: Path):
    tp = ensure_topic(tmp_path, "2026-07-27-test")
    assert tp.minutes == tmp_path / "minutes" / "2026-07-27-test" / "minutes.md"
    assert tp.scratch.is_dir()
    assert tp.snapshot.is_dir()
    assert tp.journal.parent.is_dir()
```

- [ ] **Step 3: 実行して FAIL 確認** — `python -m pytest tests/test_paths.py -v` → ModuleNotFoundError
- [ ] **Step 4: `roundtable/paths.py` を実装**

```python
"""topic ディレクトリ規約の一元管理。"""
from dataclasses import dataclass
from pathlib import Path

@dataclass(frozen=True)
class TopicPaths:
    root: Path
    minutes: Path
    journal: Path
    scratch: Path
    snapshot: Path

def topic_dir(root: Path, slug: str) -> Path:
    return root / "minutes" / slug

def ensure_topic(root: Path, slug: str) -> TopicPaths:
    d = topic_dir(root, slug)
    scratch = d / "scratch"; snapshot = d / "snapshot"
    scratch.mkdir(parents=True, exist_ok=True)
    snapshot.mkdir(parents=True, exist_ok=True)
    return TopicPaths(d, d / "minutes.md", d / "journal.json", scratch, snapshot)
```

- [ ] **Step 5: PASS 確認 → commit** — `git add -A && git commit -m "feat: scaffolding + topic paths"`

### Task 2: 意見 JSON schema 検証

**Files:**
- Create: `roundtable/schema.py`, `tests/test_schema.py`

**Interfaces:**
- Produces: `schema.validate_opinion(data: dict) -> list[str]` (空 list = 合格。文字列は違反理由)
- 契約: `{"invocation_id": str, "participant": str, "opinion": str, "claims": [{"claim": str, "evidence_type": "observed|log|diff|source|argument|none", "evidence": str}]}`。claims は 1 件以上。

- [ ] **Step 1: failing tests**

```python
from roundtable.schema import validate_opinion

VALID = {"invocation_id": "abc", "participant": "codex", "opinion": "本文",
         "claims": [{"claim": "X", "evidence_type": "argument", "evidence": "理由"}]}

def test_valid_passes():
    assert validate_opinion(VALID) == []

def test_missing_claims_fails():
    assert any("claims" in e for e in validate_opinion({**VALID, "claims": []}))

def test_bad_evidence_type_fails():
    bad = {**VALID, "claims": [{"claim": "X", "evidence_type": "vibes", "evidence": ""}]}
    assert any("evidence_type" in e for e in validate_opinion(bad))

def test_non_dict_fails():
    assert validate_opinion([]) != []
```

- [ ] **Step 2: FAIL 確認 → 実装** (`roundtable/schema.py`)

```python
"""意見 JSON の検証。stdlib のみ、fail した理由を全部返す。"""
EVIDENCE_TYPES = {"observed", "log", "diff", "source", "argument", "none"}

def validate_opinion(data) -> list[str]:
    errs: list[str] = []
    if not isinstance(data, dict):
        return ["top-level must be object"]
    for key in ("invocation_id", "participant", "opinion"):
        if not isinstance(data.get(key), str) or not data.get(key):
            errs.append(f"{key}: 非空文字列が必要")
    claims = data.get("claims")
    if not isinstance(claims, list) or len(claims) == 0:
        errs.append("claims: 1 件以上必要 (『同意します』だけは無効)")
        return errs
    for i, c in enumerate(claims):
        if not isinstance(c, dict):
            errs.append(f"claims[{i}]: object が必要"); continue
        if not isinstance(c.get("claim"), str) or not c.get("claim"):
            errs.append(f"claims[{i}].claim: 非空文字列が必要")
        if c.get("evidence_type") not in EVIDENCE_TYPES:
            errs.append(f"claims[{i}].evidence_type: {sorted(EVIDENCE_TYPES)} のいずれか")
        if not isinstance(c.get("evidence"), str):
            errs.append(f"claims[{i}].evidence: 文字列が必要")
    return errs
```

- [ ] **Step 3: PASS 確認 → commit** — `git commit -m "feat: opinion schema validation"`

### Task 3: minutes — 生成・snapshot・hash

**Files:**
- Create: `roundtable/minutes.py`, `tests/test_minutes.py`

**Interfaces:**
- Consumes: `paths.TopicPaths`
- Produces: `minutes.create(tp, topic, participants: list[str]) -> None` /
  `minutes.sha256(path: Path) -> str` / `minutes.make_snapshot(tp) -> Path` (読み取り用コピー) /
  `minutes.atomic_write(path, text) -> None` (Windows ロックに備え os.replace を retry) /
  `minutes.parse_participants(tp) -> list[str]` (frontmatter の participants 行を読む。Task 8 が使用)

- [ ] **Step 1: failing tests**

```python
from roundtable.paths import ensure_topic
from roundtable import minutes

def test_create_and_snapshot(tmp_path):
    tp = ensure_topic(tmp_path, "t1")
    minutes.create(tp, "議題X", ["codex", "cc"])
    text = tp.minutes.read_text(encoding="utf-8")
    assert "topic: 議題X" in text and "status: open" in text
    snap = minutes.make_snapshot(tp)
    assert snap.read_text(encoding="utf-8") == text
    assert minutes.sha256(snap) == minutes.sha256(tp.minutes)

def test_atomic_write_no_partial(tmp_path):
    p = tmp_path / "f.md"
    minutes.atomic_write(p, "A" * 10000)
    assert p.read_text(encoding="utf-8") == "A" * 10000

def test_parse_participants(tmp_path):
    tp = ensure_topic(tmp_path, "t1")
    minutes.create(tp, "X", ["codex", "cc"])
    assert minutes.parse_participants(tp) == ["codex", "cc"]
```

- [ ] **Step 2: FAIL 確認 → 実装**

```python
"""議事録の生成・snapshot・hash。書き込みはすべて atomic。"""
import hashlib, os, tempfile
from pathlib import Path
from .paths import TopicPaths

TEMPLATE = """---
topic: {topic}
status: open
round: 1
participants: [{participants}]
verdict:
---

# {topic}

## 背景

"""

def atomic_write(path: Path, text: str) -> None:
    """tmp に書いて os.replace。Windows の一時ロック (WinError 32) に備え短い retry 付き。"""
    import time
    fd, tmp = tempfile.mkstemp(dir=path.parent, suffix=".tmp")
    try:
        with os.fdopen(fd, "w", encoding="utf-8", newline="\n") as f:
            f.write(text)
        for attempt in range(5):
            try:
                os.replace(tmp, path); return
            except PermissionError:
                if attempt == 4: raise
                time.sleep(0.1 * (attempt + 1))
    except BaseException:
        try: os.unlink(tmp)
        except OSError: pass
        raise

def parse_participants(tp: TopicPaths) -> list[str]:
    """frontmatter の `participants: [a, b]` 行を読む (stdlib のみ・行単位の素朴 parse)。"""
    for line in tp.minutes.read_text(encoding="utf-8").splitlines():
        if line.startswith("participants:"):
            inner = line.split("[", 1)[1].rsplit("]", 1)[0]
            return [p.strip() for p in inner.split(",") if p.strip()]
    raise ValueError(f"participants 行が見つからない: {tp.minutes}")

def create(tp: TopicPaths, topic: str, participants: list[str]) -> None:
    atomic_write(tp.minutes, TEMPLATE.format(topic=topic, participants=", ".join(participants)))

def sha256(path: Path) -> str:
    return hashlib.sha256(path.read_bytes()).hexdigest()

def make_snapshot(tp: TopicPaths) -> Path:
    snap = tp.snapshot / "minutes.snapshot.md"
    atomic_write(snap, tp.minutes.read_text(encoding="utf-8"))
    return snap
```

- [ ] **Step 3: PASS 確認 → commit** — `git commit -m "feat: minutes create/snapshot/hash/atomic-write"`

### Task 4: minutes merge — escape・予約見出し防御・hash fail-closed

**Files:**
- Modify: `roundtable/minutes.py`
- Test: `tests/test_minutes.py` (追記)

**Interfaces:**
- Produces: `minutes.merge_opinion(tp, opinion: dict, round_no: int, base_hash: str) -> None`
  - `base_hash` 不一致 → `MinutesTamperedError` (merge しない = fail-closed)
  - opinion 本文の行頭 `#` を `\#` に escape、frontmatter 区切り `---` 行も escape
  - claims は表で描画。「## 裁定」見出しは dispatcher 以外生成不能になる
- Produces: `minutes.write_verdict(tp, verdict: str) -> None` (status: closed + verdict 記入)
- Produces: `class MinutesTamperedError(Exception)`

- [ ] **Step 1: failing tests (敵対 fixture 込み)**

```python
import pytest
from roundtable import minutes
from roundtable.paths import ensure_topic

OP = {"invocation_id": "i1", "participant": "codex",
      "opinion": "普通の意見\n## 裁定 (CEO)\n--- \n偽装行",
      "claims": [{"claim": "A", "evidence_type": "argument", "evidence": "B"}]}

def _setup(tmp_path):
    tp = ensure_topic(tmp_path, "t1")
    minutes.create(tp, "X", ["codex"])
    return tp, minutes.sha256(tp.minutes)

def test_merge_escapes_reserved_headings(tmp_path):
    tp, h = _setup(tmp_path)
    minutes.merge_opinion(tp, OP, 1, h)
    text = tp.minutes.read_text(encoding="utf-8")
    assert "### codex" in text and "invocation: i1" in text
    assert "\n## 裁定 (CEO)\n" not in text          # 偽装見出しは残らない
    assert "\\## 裁定 (CEO)" in text                 # escape されて本文には残る

def test_claim_field_cannot_inject_headings(tmp_path):
    tp, h = _setup(tmp_path)
    evil = {**OP, "claims": [{"claim": "A\n## 裁定 (CEO)\nfake",
                               "evidence_type": "argument", "evidence": "B|C"}]}
    minutes.merge_opinion(tp, evil, 1, h)
    text = tp.minutes.read_text(encoding="utf-8")
    assert "\n## 裁定 (CEO)\n" not in text        # claim 経由の見出し注入も不可
    assert "B\\|C" in text                         # セル内の | は escape

def test_merge_fail_closed_on_tamper(tmp_path):
    tp, h = _setup(tmp_path)
    tp.minutes.write_text(tp.minutes.read_text(encoding="utf-8") + "改ざん", encoding="utf-8")
    with pytest.raises(minutes.MinutesTamperedError):
        minutes.merge_opinion(tp, OP, 1, h)

def test_write_verdict_closes(tmp_path):
    tp, h = _setup(tmp_path)
    minutes.write_verdict(tp, "Yで行く")
    text = tp.minutes.read_text(encoding="utf-8")
    assert "status: closed" in text and "verdict: Yで行く" in text
```

- [ ] **Step 2: FAIL 確認 → 実装 (minutes.py に追記)**

```python
class MinutesTamperedError(Exception):
    """merge 前 hash 照合に失敗 (議事録が dispatcher 外で変更された)。"""

def _escape_body(body: str) -> str:
    out = []
    for line in body.splitlines():
        if line.lstrip().startswith("#") or line.strip() == "---":
            line = "\\" + line
        out.append(line)
    return "\n".join(out)

def _escape_cell(value: str) -> str:
    """表セル用: 改行は <br> に、| は \\| に。見出し注入 (改行→行頭#) を構造的に不能にする。"""
    return value.replace("|", "\\|").replace("\r", "").replace("\n", "<br>")

def merge_opinion(tp: TopicPaths, opinion: dict, round_no: int, base_hash: str) -> None:
    if sha256(tp.minutes) != base_hash:
        raise MinutesTamperedError(f"minutes hash mismatch: {tp.minutes}")
    text = tp.minutes.read_text(encoding="utf-8")
    section = [f"\n## Round {round_no}" if f"## Round {round_no}" not in text else None,
               f"\n### {opinion['participant']} (invocation: {opinion['invocation_id']})",
               "", _escape_body(opinion["opinion"]), "", "| claim | evidence_type | evidence |",
               "|---|---|---|"]
    for c in opinion["claims"]:
        section.append(f"| {_escape_cell(c['claim'])} | {c['evidence_type']} | {_escape_cell(c['evidence'])} |")
    atomic_write(tp.minutes, text + "\n".join(s for s in section if s is not None) + "\n")

def sync_round(tp: TopicPaths, round_no: int) -> None:
    """frontmatter の round: 表示を journal と同期 (CEO が minutes を直接見るため)。"""
    import re
    text = tp.minutes.read_text(encoding="utf-8")
    atomic_write(tp.minutes, re.sub(r"^round: \d+$", f"round: {round_no}", text,
                                     count=1, flags=re.MULTILINE))

def write_verdict(tp: TopicPaths, verdict: str) -> None:
    text = tp.minutes.read_text(encoding="utf-8")
    text = text.replace("status: open", "status: closed", 1)
    text = text.replace("verdict:", f"verdict: {verdict}", 1)
    atomic_write(tp.minutes, text)
```

- [ ] **Step 3: PASS 確認 → commit** — `git commit -m "feat: minutes merge with escape + tamper detection"`

### Task 5: journal — 状態機械 + 冪等 + round

**Files:**
- Create: `roundtable/journal.py`, `tests/test_journal.py`

**Interfaces:**
- Produces: `Journal.load(tp) -> Journal` / `.new_invocation(participant, round_no) -> str` (uuid) /
  `.set_state(inv_id, state, detail="")` (states: prepared|delivered|output-received|validated|merged|failed) /
  `.is_merged(inv_id) -> bool` / `.round_no -> int` / `.advance_round_if_complete(order: list[str])` /
  `.failures() -> list[dict]` / `.save()` (atomic)

- [ ] **Step 1: failing tests**

```python
from roundtable.paths import ensure_topic
from roundtable.journal import Journal

def test_invocation_lifecycle_and_idempotent_merge(tmp_path):
    tp = ensure_topic(tmp_path, "t1")
    j = Journal.load(tp)
    inv = j.new_invocation("codex", 1)
    j.set_state(inv, "merged"); j.save()
    j2 = Journal.load(tp)
    assert j2.is_merged(inv)                      # 再 merge 判定に使える

def test_round_advances_only_when_all_merged(tmp_path):
    tp = ensure_topic(tmp_path, "t1")
    j = Journal.load(tp)
    a = j.new_invocation("codex", 1); b = j.new_invocation("cc", 1)
    j.set_state(a, "merged"); j.advance_round_if_complete(["codex", "cc"])
    assert j.round_no == 1                        # cc 未完了なので据え置き
    j.set_state(b, "merged"); j.advance_round_if_complete(["codex", "cc"])
    assert j.round_no == 2

def test_failures_listed(tmp_path):
    tp = ensure_topic(tmp_path, "t1")
    j = Journal.load(tp)
    inv = j.new_invocation("codex", 1)
    j.set_state(inv, "failed", "timeout")
    assert j.failures()[0]["detail"] == "timeout"
```

- [ ] **Step 2: FAIL 確認 → 実装** (uuid4、json、minutes.atomic_write を再利用。round_no は journal.json 内に保持。約 70 行)

```python
"""invocation 状態機械。journal.json に atomic 保存、merge 冪等の根拠。"""
import json, uuid
from .minutes import atomic_write
from .paths import TopicPaths

STATES = {"prepared", "delivered", "output-received", "validated", "merged", "failed"}

class Journal:
    def __init__(self, tp: TopicPaths, data: dict):
        self.tp = tp; self.data = data

    @classmethod
    def load(cls, tp: TopicPaths) -> "Journal":
        if tp.journal.exists():
            return cls(tp, json.loads(tp.journal.read_text(encoding="utf-8")))
        return cls(tp, {"round": 1, "invocations": {}})

    @property
    def round_no(self) -> int: return self.data["round"]

    def new_invocation(self, participant: str, round_no: int) -> str:
        inv = uuid.uuid4().hex[:12]
        self.data["invocations"][inv] = {"participant": participant, "round": round_no,
                                          "state": "prepared", "detail": ""}
        self.save(); return inv

    def set_state(self, inv: str, state: str, detail: str = "") -> None:
        if state not in STATES:
            raise ValueError(f"unknown state: {state}")
        rec = self.data["invocations"][inv]
        rec["state"] = state; rec["detail"] = detail
        self.save()

    def is_merged(self, inv: str) -> bool:
        return self.data["invocations"].get(inv, {}).get("state") == "merged"

    def advance_round_if_complete(self, order: list[str]) -> None:
        r = self.round_no
        done = {v["participant"] for v in self.data["invocations"].values()
                if v["round"] == r and v["state"] == "merged"}
        if set(order) <= done:
            self.data["round"] = r + 1; self.save()

    def failures(self) -> list[dict]:
        return [{"invocation": k, **v} for k, v in self.data["invocations"].items()
                if v["state"] == "failed"]

    def save(self) -> None:
        atomic_write(self.tp.journal, json.dumps(self.data, ensure_ascii=False, indent=1))
```

- [ ] **Step 3: PASS 確認 → commit** — `git commit -m "feat: journal state machine"`

### Task 6: packet 生成 + Tier3 relay (クリップボード)

**Files:**
- Create: `roundtable/packet.py`, `tests/test_packet.py`

**Interfaces:**
- Consumes: `TopicPaths`, invocation_id
- Produces: `packet.build(tp, participant, inv_id, role_hint="") -> str` /
  `packet.to_clipboard(text) -> None` (clip.exe UTF-16LE, テストでは呼ばない)

- [ ] **Step 1: failing test**

```python
from roundtable.paths import ensure_topic
from roundtable.packet import build

def test_packet_contains_contract(tmp_path):
    tp = ensure_topic(tmp_path, "t1")
    text = build(tp, "codex", "inv123", role_hint="実装視点で")
    assert "inv123" in text
    assert str(tp.snapshot / "minutes.snapshot.md") in text
    assert str(tp.scratch / "inv123.json") in text
    assert "invocation_id" in text and "claims" in text     # 出力契約の説明
    assert "tmp" in text.lower()                             # tmp→rename 指示
```

- [ ] **Step 2: FAIL 確認 → 実装**

```python
"""議題 packet の生成とクリップボード搬出 (Tier3)。"""
import subprocess
from .paths import TopicPaths

_TEMPLATE = """[roundtable packet / invocation: {inv}]
あなたは roundtable の参加者 ({participant}) です。{role_hint}
1. 議事録スナップショットを読む: {snapshot}
2. 意見を JSON で書く。まず一時ファイル {out}.tmp に書き、完成後 {out} にリネームする。
   形式: {{"invocation_id": "{inv}", "participant": "{participant}",
          "opinion": "本文 (markdown 可)",
          "claims": [{{"claim": "主張", "evidence_type": "observed|log|diff|source|argument|none",
                      "evidence": "根拠"}}]}}
   claims は 1 件以上必須。「同意します」だけの応答は無効として却下されます。
3. 議事録本体や他のファイルは変更しないこと。"""

def build(tp: TopicPaths, participant: str, inv_id: str, role_hint: str = "") -> str:
    out = tp.scratch / f"{inv_id}.json"
    snap = tp.snapshot / "minutes.snapshot.md"
    return _TEMPLATE.format(inv=inv_id, participant=participant, role_hint=role_hint,
                            snapshot=snap, out=out)

def to_clipboard(text: str) -> None:
    # BOM 必須: clip.exe はパイプ入力のエンコーディング判定を先頭 \xff\xfe に依存する
    subprocess.run(["clip.exe"], input=(chr(0xFEFF) + text).encode("utf-16-le"), check=True)
```

- [ ] **Step 3: PASS 確認 → commit** — `git commit -m "feat: packet builder + clipboard relay (tier3)"`

### Task 7: watcher — 出現検知 → 検証 → merge 駆動

**Files:**
- Create: `roundtable/watcher.py`, `tests/test_watcher.py`

**Interfaces:**
- Consumes: `schema.validate_opinion`, `minutes.merge_opinion/sha256/make_snapshot`, `Journal`
- Produces: `watcher.collect(tp, journal, inv_id, participant, timeout_s=900, poll_s=2.0, clock=time) -> dict`
  - 戻り値: `{"ok": True}` or `{"ok": False, "reason": "<分類>"}`。分類: timeout / parse / schema / id-mismatch / tampered
  - 成功時: journal を merged、失敗時 failed にする。merge 済み inv は再 merge しない (冪等)

- [ ] **Step 1: failing tests (mock 席: テストが直接ファイルを置く)**

```python
import json
from roundtable.paths import ensure_topic
from roundtable import minutes, watcher
from roundtable.journal import Journal

def _setup(tmp_path):
    tp = ensure_topic(tmp_path, "t1")
    minutes.create(tp, "X", ["codex"]); minutes.make_snapshot(tp)
    j = Journal.load(tp)
    inv = j.new_invocation("codex", 1)
    return tp, j, inv

def _opinion(inv, participant="codex"):
    return {"invocation_id": inv, "participant": participant, "opinion": "意見",
            "claims": [{"claim": "A", "evidence_type": "argument", "evidence": "B"}]}

def test_collect_success(tmp_path):
    tp, j, inv = _setup(tmp_path)
    (tp.scratch / f"{inv}.json").write_text(json.dumps(_opinion(inv)), encoding="utf-8")
    assert watcher.collect(tp, j, inv, "codex", timeout_s=1)["ok"]
    assert j.is_merged(inv)
    assert "### codex" in tp.minutes.read_text(encoding="utf-8")

def test_collect_timeout(tmp_path):
    tp, j, inv = _setup(tmp_path)
    r = watcher.collect(tp, j, inv, "codex", timeout_s=0.1, poll_s=0.05)
    assert r == {"ok": False, "reason": "timeout"}
    assert j.failures()

def test_collect_id_mismatch(tmp_path):
    tp, j, inv = _setup(tmp_path)
    (tp.scratch / f"{inv}.json").write_text(json.dumps(_opinion("other")), encoding="utf-8")
    assert watcher.collect(tp, j, inv, "codex", timeout_s=1)["reason"] == "id-mismatch"

def test_collect_detects_tamper(tmp_path):
    tp, j, inv = _setup(tmp_path)
    tp.minutes.write_text(tp.minutes.read_text(encoding="utf-8") + "改ざん", encoding="utf-8")
    # snapshot 採取後に原本が変わった → merge は fail-closed
    (tp.scratch / f"{inv}.json").write_text(json.dumps(_opinion(inv)), encoding="utf-8")
    assert watcher.collect(tp, j, inv, "codex", timeout_s=1)["reason"] == "tampered"
```

- [ ] **Step 2: FAIL 確認 → 実装**

```python
"""scratch 監視 → 検証 → merge。dispatcher の回収側の心臓部。"""
import json, time
from .paths import TopicPaths
from . import minutes as m
from .schema import validate_opinion
from .journal import Journal

def collect(tp: TopicPaths, journal: Journal, inv_id: str, participant: str,
            timeout_s: float = 900, poll_s: float = 2.0, clock=time) -> dict:
    if journal.is_merged(inv_id):
        return {"ok": True}                       # 冪等
    base_hash = m.sha256(tp.snapshot / "minutes.snapshot.md")
    out = tp.scratch / f"{inv_id}.json"
    deadline = clock.monotonic() + timeout_s
    while not out.exists():
        if clock.monotonic() >= deadline:
            journal.set_state(inv_id, "failed", "timeout")
            return {"ok": False, "reason": "timeout"}
        clock.sleep(poll_s)
    journal.set_state(inv_id, "output-received")
    data = None
    for attempt in range(3):                      # grace period: 部分書き込み・AV の一時ロック吸収
        try:
            data = json.loads(out.read_text(encoding="utf-8"))
            break
        except (json.JSONDecodeError, UnicodeDecodeError, OSError):
            clock.sleep(0.2)
    if not isinstance(data, dict):                # parse 不能 or 非 dict (配列/null 等) をまとめて弾く
        journal.set_state(inv_id, "failed", "parse")
        return {"ok": False, "reason": "parse"}
    if data.get("invocation_id") != inv_id or data.get("participant") != participant:
        journal.set_state(inv_id, "failed", "id-mismatch")
        return {"ok": False, "reason": "id-mismatch"}
    errs = validate_opinion(data)
    if errs:
        journal.set_state(inv_id, "failed", "schema: " + "; ".join(errs))
        return {"ok": False, "reason": "schema"}
    journal.set_state(inv_id, "validated")
    try:
        m.merge_opinion(tp, data, journal.round_no, base_hash)   # snapshot 時 hash を渡す (再計算しない)
    except m.MinutesTamperedError:
        journal.set_state(inv_id, "failed", "tampered")
        return {"ok": False, "reason": "tampered"}
    journal.set_state(inv_id, "merged")
    return {"ok": True}
```

注: `merge_opinion` へは **snapshot 採取時の base_hash をそのまま渡す** (その場で再計算した
hash を渡すと照合が常に一致し検知が無力化する — 内部レビュー #2 で検出された罠)。
これにより「参加者ターン中の原本改変」は merge 内部の 1 回の照合で fail-closed になる。

- [ ] **Step 3: PASS 確認 → commit** — `git commit -m "feat: watcher collect with validation + tamper fail-closed"`

### Task 8: cli — new-topic / dispatch / status / close

**Files:**
- Create: `roundtable/cli.py`, `tests/test_cli.py`

**Interfaces:**
- Produces (argparse, `python -m roundtable.cli <cmd>`):
  - `new-topic <slug> --topic <題> --participants codex,cc` → minutes 生成
  - `dispatch <slug> --participant codex [--role-hint ...] [--no-clipboard] [--timeout SEC]` →
    snapshot 更新 + invocation 発行 + packet 生成 (stdout & クリップボード) → **collect まで実行**。
    クリップボード搬出成功時に `journal.set_state(inv, "delivered", "tier3")` を記録
  - `status <slug>` → round / 各 invocation 状態を表形式 stdout
  - `close <slug> --verdict <裁定>` → **失敗一覧を stdout に必ず表示**してから verdict 記入
- 設計注: dispatch は AI を実行しない。packet を出して待つだけ (Tier3)

- [ ] **Step 1: failing tests (subprocess ではなく main(argv) を直接呼ぶ)**

```python
import json
from roundtable.cli import main
from roundtable.paths import ensure_topic

def test_new_topic_and_status(tmp_path, capsys):
    main(["new-topic", "t1", "--topic", "X", "--participants", "codex,cc",
          "--root", str(tmp_path)])
    main(["status", "t1", "--root", str(tmp_path)])
    out = capsys.readouterr().out
    assert "round: 1" in out

def test_dispatch_no_clipboard_and_close_shows_failures(tmp_path, capsys, monkeypatch):
    main(["new-topic", "t1", "--topic", "X", "--participants", "codex",
          "--root", str(tmp_path)])
    # collect を即 timeout させる
    main(["dispatch", "t1", "--participant", "codex", "--no-clipboard",
          "--timeout", "0.1", "--root", str(tmp_path)])
    main(["close", "t1", "--verdict", "見送り", "--root", str(tmp_path)])
    out = capsys.readouterr().out
    assert "timeout" in out       # 失敗一覧が隠れず表示される
```

- [ ] **Step 2: FAIL 確認 → 実装** (argparse で 4 subcommand を素直に。dispatch = snapshot →
  journal.new_invocation → packet.build → (clipboard 成功時 `set_state(inv, "delivered", "tier3")`)
  → watcher.collect → 成功時 `journal.advance_round_if_complete(participants)` +
  `minutes.sync_round(tp, journal.round_no)` → 結果表示。participants は
  `minutes.parse_participants(tp)` (Task 3) で読む。timeout 失敗時は「未貼り付け?」の
  確認メッセージを表示 (delivered 記録があるため区別可能)。
  close = `journal.failures()` を表示 → `minutes.write_verdict`。約 100 行)
- [ ] **Step 3: PASS 確認 → commit** — `git commit -m "feat: cli new-topic/dispatch/status/close"`

### Task 9: E2E mock + adversarial 一式

**Files:**
- Create: `tests/test_e2e_mock.py`

- [ ] **Step 1: E2E テストを書く** (mock 席 = 別スレッドで scratch に置く)

```python
import json, threading, time
from roundtable.cli import main
from roundtable.paths import ensure_topic, topic_dir

def test_full_topic_two_rounds(tmp_path, capsys):
    main(["new-topic", "t1", "--topic", "X", "--participants", "codex",
          "--root", str(tmp_path)])
    for rnd in (1, 2):
        tp = ensure_topic(tmp_path, "t1")
        def seat():
            # packet が指す出力先 = 最新 invocation。sleep 決め打ちでなく journal を poll (race 回避)
            inv = None
            deadline = time.monotonic() + 5
            while inv is None and time.monotonic() < deadline:
                if tp.journal.exists():
                    j = json.loads(tp.journal.read_text(encoding="utf-8"))
                    pend = [k for k, v in j["invocations"].items() if v["state"] != "merged"]
                    inv = pend[-1] if pend else None
                if inv is None:
                    time.sleep(0.05)
            assert inv is not None, "invocation が発行されなかった"
            (tp.scratch / f"{inv}.json").write_text(json.dumps(
                {"invocation_id": inv, "participant": "codex",
                 "opinion": f"round{rnd} の意見",
                 "claims": [{"claim": "C", "evidence_type": "argument", "evidence": "E"}]}),
                encoding="utf-8")
        t = threading.Thread(target=seat); t.start()
        main(["dispatch", "t1", "--participant", "codex", "--no-clipboard",
              "--timeout", "5", "--root", str(tmp_path)])
        t.join()
    text = (topic_dir(tmp_path, "t1") / "minutes.md").read_text(encoding="utf-8")
    assert "round1 の意見" in text and "round2 の意見" in text
    main(["close", "t1", "--verdict", "採用", "--root", str(tmp_path)])
    assert "status: closed" in (topic_dir(tmp_path, "t1") / "minutes.md").read_text(encoding="utf-8")
```

- [ ] **Step 2: 全テスト実行 → PASS 確認** — `python -m pytest -v` (全 suite)
- [ ] **Step 3: commit** — `git commit -m "test: e2e mock seat over two rounds"`

### Task 10: PROTOCOL.md + CC アダプタ skill

**Files:**
- Create: `docs/PROTOCOL.md`, `adapters/cc-skill/SKILL.md`

- [ ] **Step 1: PROTOCOL.md を書く** — ホスト AI 向け手順書。内容 (DESIGN v5 §5 を運用文に):
  議題開始 (`new-topic`) → CEO の指名を受けて `dispatch` → Tier3 なら「クリップボードに
  packet を入れた。<席チャット名> に貼ってください」と CEO に 1 行で伝える → collect 結果の
  raw セクションを提示 → ラウンド境界で CEO に確認 → `close --verdict`。
  禁止事項 (ホストは意見を言わない / minutes を直接編集しない / AI を subprocess で呼ばない) を明記
- [ ] **Step 2: adapters/cc-skill/SKILL.md を書く** — frontmatter (name: roundtable,
  description: 「/roundtable で壁打ち司会補佐を開始…」) + 本文は「docs/PROTOCOL.md を読み、
  そのとおりに進行する。scripts は `python -m roundtable.cli`」のみ (SSOT は PROTOCOL.md)
- [ ] **Step 3: commit** — `git commit -m "docs: PROTOCOL + cc-skill adapter"`

## Phase 1.5 — spike (docs/spike-plan.md を実施)

- [ ] Spike 1 (Codex app-server): 実施 → `docs/spike-results.md` に記録
- [ ] Spike 2 (CCD send_message): 実施 → 同上
- [ ] seats.json の tier 決定。**両方 no-go なら Phase 2 をスキップして v0.1 完了へ**

## Phase 2 — Tier1 relay (spike 通過席のみ)

### Task 11: relay_ccd (CC 席の自動 relay) — 条件付き

**Files:**
- Create: `roundtable/relay_ccd.py`

- [ ] ホスト (CC) が MCP ツール `send_message` で席セッションへ packet を送る手順を
  PROTOCOL.md に追記 (Python からではなくホストの tool call で行うため、実装は手順書 +
  cli に `--tier1-note` 表示を足すのみ)。E2E は実席で手動 1 回
- [ ] commit — `git commit -m "feat: tier1 relay via ccd send_message (protocol)"`

### Task 12: relay_codex (Codex 席の自動 relay) — 条件付き

**Files:**
- Create: `roundtable/relay_codex.py`, `tests/test_relay_codex.py`

- [ ] spike の実測プロトコル (daemon socket への JSON-RPC: initialize → thread/resume →
  turn/start) をクライアント関数 `send_turn(thread_ref, text) -> None` として実装。
  socket 接続のみ、プロセスを起動しない (daemon 不在時は「setup 手順を CEO に提示」して fail)
- [ ] mock server (ローカル socket) でユニットテスト → commit

## v0.1 受け入れ (DESIGN v5 §10)

- [ ] 実議題 1 本を codex + cc の実席で複数 round (Tier は spike 結果どおり)
- [ ] 失敗が隠れないこと・改ざん検知・CEO 操作が判断のみ (Tier3 は貼り付け込み) を確認
- [ ] チャット履歴がアプリに残っていることを CEO が確認
- [ ] 結果を docs/ に記録して tag `v0.1`
